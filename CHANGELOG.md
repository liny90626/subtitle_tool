# 更新记录

## v0.3.0

- 体积：去掉打包里用不到的东西——Qt 的 QML/Quick 一整套（查依赖才发现是被虚拟键盘输入法
  插件顺带拖进来的）、软件 OpenGL 兜底 `opengl32sw`、`Qt6Pdf`，以及 `hf_xet`（没有它下载
  自动退回普通 HTTP，实测一样快）。删不掉的也记下来了：PyAV 的 x265/SvtAv1Enc 虽然是视频
  编码器，但 libavcodec 对它们是硬依赖
- 性能：**翻译按长度排序后再切批**。CTranslate2 要把一批里每条补齐到最长那条再算，而字幕
  正是「是。」和整句长台词混在一起，白算一堆 padding。实测快 16%，逐条比对译文完全一致
- 性能：音轨解码就地归一化，一小时的音轨少一次 230MB 的全量拷贝
- 实测（Windows，构建日志可查）：压缩包 129MB → 109MB，解压后 326MB → 280MB
- 量过但**没改**：转写的 batch_size（8/16/24 全落在噪声里）、让出一个核给界面（4 核机上
  代价约等于零，说明 v0.2.0 那个取舍是免费的）

## v0.2.1

- 修复：**v0.2.0 处理到 7% 左右直接闪退，还什么线索都不留**。窗口模式下 PySide6 一碰到
  槽函数里的异常就结束进程——v0.2.0 把缺席的标准流补上之后，原本的「卡死」正好变成了
  「闪退」。现在槽函数、拖放、定时回调统统兜住异常，未捕获的异常写进
  `%APPDATA%\subtitle-tool\subtitle-tool.log`，界面上也会显示
- 修复：模型下载的进度回调是从 huggingface_hub / hf_xet 自己的线程里调过来的，原本在
  那些线程里直接发 Qt 信号。改成只记状态、界面定时来取，不在 Qt 没见过的线程上碰 Qt
- 加固：覆盖安装的清理加了保险——要删的超过发布清单的一半就整个放弃，免得清单跟目录
  对不上时把安装目录清空
- 加固：双核及以下的机器不再让出核心（原来会让转写慢一倍）
- 测试：冒烟测试改成在**解压出来的副本**上真跑一整条流水线（下模型 → 转写 → 翻译 →
  写字幕），并验证覆盖升级后的清理。原来只验 `--list-tracks` 和开窗，压根碰不到
  CTranslate2 / onnxruntime 的加载——这次的问题就是从这个缺口漏过去的

## v0.2.0

- 修复：**下载翻译模型时整个界面卡死**，进度一直停在语音转写 69%，点一下就提示未响应。
  根因是窗口模式下 `sys.stderr` 是 None，huggingface_hub 的进度条往那儿一写就抛异常，
  异常冒进 Qt 槽函数、打印它又要用 stderr，进程就此卡住。现在缺席的标准流会换成不会炸
  的替身，下载进度也改由程序自己上报
- 新增：**模型下载有进度了**。620MB 的翻译模型不再一声不吭，进度条会走，中途也能取消；
  下载失败会停掉整批，不再每个文件重试一遍
- 新增：**解压覆盖原目录即可升级**。打包时写一份发布清单，启动时照清单清掉旧版残留的
  DLL / .pyd（只动程序自己的 `_internal` 目录）。模型缓存在用户目录下，升级不碰它，
  不用重下
- 改进：转写时留一个核给界面。占满全部核心时 Windows 会直接把窗口判成「未响应」，
  而且 CTranslate2、VAD 和界面线程一起抢核并不会更快
- 改进：**窗口出得更快**。CTranslate2 和 huggingface_hub 改成用到才加载，显卡探测挪到
  窗口显示之后，开窗不再等这两个大家伙。（同时排掉了 Qt 里用不到的几十个模块，但实测
  体积没变——PySide6 的打包钩子本来就只收被 import 的模块，这些排除项是空转。
  326MB 的大头在哪，构建日志里现在会列出来。）

## v0.1.4

- 新增：**中英双语界面**。图形界面和命令行的全部文案——含 99 种语种名、报错提示和
  命令行帮助——都有中英两套，默认跟随系统语言，界面里随时可切且即时生效（文件列表和
  已选设置都留着），命令行用 `--lang zh|en`
- 新增：README 分中英两份（[README.md](README.md) / [README.en.md](README.en.md)）
- 改进：设置（下载源、代理、界面语言）统一收进 `settings.py`，图形界面与命令行共用一份

## v0.1.3

- 修复：打包版在国内网络下**根本下不到模型**，报 `ConnectTimeout: [WinError 10060]`
  和「cannot find the appropriate snapshot folder」，看着像视频有问题，其实是连不上
  huggingface.co。现在官方源连不上会自动换到 `hf-mirror.com` 镜像，设置里也能手动
  指定下载源或填代理（命令行对应 `--model-source` / `--proxy`），设置图形界面与
  命令行共用一份
- 改进：模型下过一次之后完全走本地缓存，不再每次启动都去问一次 Hub——离线启动快得多，
  断网时也不会卡在超时上
- 改进：下载失败时给出照着做就能解决的提示（换镜像 / 填代理 / 手动拷模型），并写明
  用的是哪个下载源，不再只甩一段英文调用栈

## v0.1.2

程序本身与 v0.1.1 完全相同，只修了发布流程：仓库的 `GITHUB_TOKEN` 默认只读，
创建 Release 需要在工作流里显式提权到 `contents: write`，否则最后一步会报
`Resource not accessible by integration`。

## v0.1.1

- 修复：模型加载失败（断网、磁盘不够）时图形界面会永久卡在「运行中」，
  开始按钮再也点不动，只能杀进程
- 修复：命令行批处理时单个坏文件（没有音轨、音轨号越界）会抛栈中断整批，
  后面排队的文件全不处理；现在逐文件报错后继续，退出码如实反映失败

## v0.1.0

首个可用版本。

- 多音轨探测与选轨：列出片源里每条音轨的编解码器、声道、语言标签
- 语音识别：faster-whisper(CTranslate2)，99 语种自动识别，支持一条音轨内
  多语言混说的逐段识别
- 翻译：NLLB-200-distilled-600M，99 种目标语言，译文/原文/双语三种排版
- 输出 SRT / VTT / TXT；按词级时间戳切分成字幕大小的条目
- 图形界面支持拖拽导入与批量处理，可随时取消；命令行支持脚本化
- Windows 免安装包约 500MB，无 PyTorch 依赖，不需要另装 FFmpeg
